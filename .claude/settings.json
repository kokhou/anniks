{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '\\.(gs|html)$' && cd /Users/choiai/Documents/projects/Anniks/anniks && clasp push --force; } 2>/dev/null || true",
            "statusMessage": "Pushing to Google Sheets..."
          }
        ]
      }
    ]
  }
}
